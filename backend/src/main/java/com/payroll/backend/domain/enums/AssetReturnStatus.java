package com.payroll.backend.domain.enums;

/** Outcome recorded by HR for an asset released to an exiting employee. */
public enum AssetReturnStatus {
    PENDING,
    RETURNED,
    NOT_RETURNED,
    NOT_REQUIRED
}
